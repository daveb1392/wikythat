# Optional: API Analytics key from apianalytics.dev
ANALYTICS_KEY=your_analytics_key_here

# Required: Secret key for /health endpoint access
HEALTH_SECRET=your_secret_key_here

# Required: API secret key for authenticating requests to /page, /sitemap-index, /sitemap
API_SECRET_KEY=your_api_secret_key_here

# Required: Supabase credentials (for sync_slugs.py script)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Set when deploying to Vercel (disables static file mounting)
# VERCEL=1
